# Decision Records - Environment Configuration
# Copy this file to .env and customize values
#
# REQUIRED: Generate a secure secret key
# Run: openssl rand -hex 32

SECRET_KEY=your-secret-key-here

# DATABASE (optional - defaults to SQLite)
# For PostgreSQL (recommended for production):
# DATABASE_URL=postgresql://user:password@localhost:5432/decisions
#
# For SQLite (default):
# DATABASE_URL=sqlite:////data/architecture_decisions.db

# PostgreSQL Password (when using docker-compose --profile postgres)
POSTGRES_PASSWORD=changeme

# EMAIL NOTIFICATIONS (optional)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# MAIL_FROM=your-email@gmail.com

# AUTHENTICATION (optional)
# For OIDC/SSO - configure per-tenant in admin settings

# EDITION
# community = Open source features only
# enterprise = All features (requires ee/ submodule)
DECISION_RECORDS_EDITION=community

# SELF-HOSTING
# Always set to true for self-hosted deployments
SKIP_CLOUDFLARE_CHECK=true
